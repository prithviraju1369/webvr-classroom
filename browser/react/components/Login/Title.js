import React from 'react';

/* ----------------- COMPONENT ------------------ */

export default (props) => (
  <div style={props.styles.aboutContainer} >
    <h1 style={props.styles.appTitle}>V Meetup</h1>
  </div>
);
